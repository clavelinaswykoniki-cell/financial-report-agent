# Tasks

## Done

- 2026-06-24: 按用户确认的效果图调整 dashboard 首屏顺序：删除顶部大号“当前 a 值/点位数/休市提示”统计块，把四联行情卡提升到顶部，把历史曲线移动到四联卡下方；公式、行情源、阈值和飞书逻辑未改。
- 2026-06-24: 增加 HTML 回归测试，确认 `quote-section` 在 `chart-panel` 前、旧 `primary-value/latestA` 不再出现在页面模板里；完整 `tests.test_511130_live_monitor` 通过 91 个测试。
- 2026-06-24: 看板新增三只证券行情卡片，展示最新价、涨跌、成交、五档买卖和每只证券本地分时小曲线；五档盘口来自新浪展示快照，不参与 a 值公式或飞书预警。
- 2026-06-24: 行情区改成四张横向连在一起的卡片：`511130`、`019776`、`019837`、`套利值A`；桌面端卡片零间距相连，窄屏保持横向滚动，不再竖向堆叠。
- 2026-06-24: 默认 dashboard / Docker / Railway 自动刷新间隔改为 3 秒；严格实时 a 仍保留东方财富快照、3 秒同步和 30 秒新鲜度 fail-closed 规则。
- 2026-06-24: 增加五档盘口解析、行情卡片合成和四联横排 HTML 回归测试，完整 `tests.test_511130_live_monitor` 通过 91 个测试。
- 2026-06-24: 已推送 GitHub 并部署 Railway production `7ab73f6a-ce74-4ec2-ac7f-d9c7311a13e9`；线上只读 smoke 通过，生产 DOM 验收为 4 张联排卡、3 条分时、30 行盘口、`adjacentGap=0`。
- 2026-06-23: 生成 511130 `20260615` 至 `20260618` 的 1 分钟 `estimated_a` / `actual_a` 汇总 CSV 和概览 SVG；`20260619` 因休市未纳入。
- 2026-06-23: 生成 511090 `20260608` 至 `20260618` 的日收盘 `estimated_a` / `actual_a` CSV、summary 和概览 SVG；本次明确标注为日收盘口径，不是分钟级曲线。
- 2026-06-23: 预警阈值改为 `±300`、`±500`，按 `abs(a)` 判断，且正负方向使用独立阈值状态。
- 2026-06-23: 修复历史 seed 与 live `RUNS_DIR` 同日曲线读取没有真正合并的问题；现在 seed 先入、live 同 timestamp 覆盖。
- 2026-06-23: 增加 seed/live 合并回归测试，完整 `tests.test_511130_live_monitor` 通过 88 个测试。
- 2026-06-23: 启动两个只读 Debug 检查员：一个查代码风险，一个抽查 A 值准确性；A 值抽查与报告一致，代码高优问题已修。
- 2026-06-19: 增加历史开市日曲线回看；曲线接口可读历史报告源，当前 a 和飞书预警仍只用严格实时源。
- 2026-06-19: 新增 `seed_history_curves.py`，并内置 `20260612`、`20260615`、`20260616`、`20260617` 四天 1 分钟 `estimated_a` 历史种子；`20260618` 未找到完整曲线文件，未补。
- 2026-06-19: 曲线控件默认 `全天`，无今日实时点时自动选最近历史开市日，并在图表标题区分 `今天实时` / `历史回放`。
- 2026-06-19: 修复端午休市误报：`config.json` 新增 2026 年上交所官方休市日，自动计算门禁在 `20260619` 返回休市暂停并等到 `20260622 09:25`，不再把休市日 PCF 缺失累计成运行异常。
- 2026-06-19: 按用户要求关闭自动运行错误飞书通知；缺利息、PCF未就绪、行情不同步等错误只在页面/`/health`/日志诊断，不发群。
- 2026-06-19: 代码层断掉错误通知旁路：dashboard 自动失败、CLI `precheck --notify`、CLI 主异常和 once-mode 无阈值结果都不能发送飞书。
- 2026-06-19: 关闭无阈值的“运行检查”飞书消息；飞书只发真正触发阈值的 `511130 a值预警` 和显式手工 `/api/notify-test`。
- 2026-06-19: 有效阈值预警增加同轮发送重试，默认 3 次；全部失败时回滚阈值激活状态，下一轮仍会继续尝试。
- 2026-06-19: 严格实时当前 a 和飞书预警锁定单一信源 `realtime_eastmoney`；东方财富不可用或不同步时 fail closed，不用新浪/分钟线兜底触发预警。
- 2026-06-19: 严格实时行情失败但 PCF/逐券利息上下文已就绪时，新增“降级行情候选预警”兜底；只在穿阈值时发飞书，标题和正文明确标注候选/降级，不污染当前严格 a。
- 2026-06-19: 正式预警和候选预警使用独立阈值去重状态，避免两条链路切换时重复刷屏。
- 2026-06-19: `/health` 和 `/api/data.config` 新增 `alert_policy_setup`，只读暴露错误不通知、无阈值不通知、降级候选启用和通知重试策略；`smoke_check.py` 会校验这些策略是否已上线。
- 2026-06-19: 已按用户授权部署 Railway production，部署 `d97fbc14-846c-4d80-8dfc-bd9f93b369ab`；只读 smoke 和 `/health` 验证通过，未发送飞书测试消息。
- 2026-06-19: 自动循环进入交易时段后先准备并缓存 PCF + 逐券利息上下文，成功后每轮只刷新东方财富实时价和 a 值。
- 2026-06-19: 新增同交易日官方利息缓存兜底；同一天已验证过的逐券利息可在上交所取息短暂失败时复用，跨日期缓存仍拒绝用于当前 a。
- 2026-06-17: 新增 `daily_actual_a_report.py`，可在下一交易日 PCF 发布后生成前一交易日 1 分钟预估 a / 实际 a 的 CSV、PDF、`summary.json` 和 raw 留痕。
- 2026-06-17: 日报脚本默认用运行日 PCF 的 `PreTradingDay` 自动识别目标日；预估 a 用目标日 `EstimatedCashComponent`，实际 a 用运行日 `PreCashComponent` 回填。
- 2026-06-17: 日报脚本 fail closed：PCF 未发布、成分券变化、申赎单位变化、逐券利息缺失或 240 个共同 1 分钟点不足时，只写 pending 说明，不输出正式数值。
- 2026-06-17: 日报脚本新增 5 分钟交叉核验 CSV/PDF，默认用新浪 5 分钟 K 线 48 个共同点；它不替代 1 分钟正式主口径。
- 2026-06-17: 增加日报 fixture tests：2026-06-12 收盘预估 `+297.37` / 实际 `-0.27`，2026-06-15 收盘预估 `+234.96` / 实际 `-254.61`。
- 2026-06-17: PDF 生成改用嵌入 macOS 中文字体，已用 PNG 渲染检查标题、图例、现金项、利息来源、收盘 a、均值、区间和结论可读。
- 2026-06-17: 已创建 Codex 本机 cron 自动化 `511130-a` / `511130 次日实际a日报`，工作日早上运行，脚本内部重试到 10:00。
- 2026-06-16: 已按用户授权部署 Railway 稳定性修复到生产，最新验证部署 `6ad1ba11-56b8-4a75-a52c-43252aa79673` Online。
- 2026-06-16: 已更新 Railway 飞书 webhook 和签名密钥环境变量；真实 `POST /api/notify-test` 返回飞书业务码 `0`、`success`，`/health` 显示 `飞书正常`。原始 webhook/secret 未写入仓库文档。
- 2026-06-16: 生产只读 smoke 检查通过：`/health`、`/api/data` 均 200，`process_ok=true`、`auto_loop=running`、飞书配置可见、严格实时准确性护栏可见。
- 2026-06-16: 本地修复 Railway 运行稳定性：自动日期与跨日滚动、交易时段自动计算门禁、盘外跳过启动预加载、Eastmoney 系统 curl fallback、15秒轮询、health 数据状态字段。
- 2026-06-16: 自动线程增加外层守护，单轮非预期异常会记录并继续下一轮，避免页面在线但自动计算/飞书提醒线程悄悄退出。
- 2026-06-16: `/health` 增加 `auto_loop` 心跳诊断，区分自动线程运行中、未启动、卡住或禁用。
- 2026-06-16: `/health` 增加 `process_ok`，并且只在 `auto_loop.code=stale` 时返回 HTTP 503，让 Railway 重启卡住的自动线程；行情/PCF不可用仍返回 200。
- 2026-06-16: `/health` 和 `/api/data.config` 增加脱敏 `notification_setup`，可在不发飞书消息的情况下确认 webhook、kind、Feishu secret 是否被 Railway 看到。
- 2026-06-16: `/health` 和 `/api/data.config` 增加 `accuracy_setup`，显式暴露公式版本、PCF来源、成分券锁定、严格实时源、3秒同步、30秒新鲜度和缺利息不兜底策略。
- 2026-06-16: 新增只读 `smoke_check.py`，部署后可检查 `/health`、`/api/data`、自动线程、飞书配置诊断和准确性护栏，不触发飞书消息。
- 2026-06-16: 计算成功但飞书发送失败时，dashboard 保留当前严格实时 a，并把飞书失败隔离到 notification 诊断；`/health.data_ok` 不再被通知类错误污染。
- 2026-06-16: `/health` 跨日只做轻量日期滚动，不触发 PCF/利息外部预加载，避免 Railway 健康检查被上游数据源拖慢。
- 2026-06-16: 飞书/企业微信通知成功判定必须有业务响应码，避免 HTTP 200 但空响应/非业务成功被误判为已发送。
- 2026-06-16: `a_values.jsonl`、`alerts.jsonl`、`notifications.jsonl` 和 `runs/` 日期索引读取 fail-soft，Volume 上单个文件损坏或类型异常不会让 `/api/data`、`/health` 或 `/api/dates` 直接 500。
- 2026-06-16: 历史点和成分券展示字段类型清洗，坏 `timestamp`、数字型组件 `code/name` 等异常展示字段不会让图表排序、公式快照或组件表格崩掉。
- 2026-06-16: API 路由增加结构化 JSON 错误兜底，非预期展示层异常不会让前端拿到断开的连接。
- 2026-06-16: 增加 focused tests，覆盖自动日期/跨日滚动、`/health` 轻量滚动、fixed 日期不被 auto 覆盖、交易时段门禁、盘外跳过预加载、自动线程异常恢复和心跳诊断、stale 自动线程 healthcheck 503、飞书配置脱敏诊断、准确性护栏诊断、只读 smoke 检查、API 异常 JSON 兜底、webhook 业务响应码校验、飞书签名时间戳、PCF未就绪状态/退避、三层 diagnostics、Eastmoney fallback、自动错误不通知、独立飞书测试、通知/数据健康隔离、坏展示字段、通知/结果/状态日志读写失败、进程内当前a兜底、`state.json` 容错、保存配置不意外冻结 auto 日期。
- 2026-06-15: 部署 Railway 公网看板，URL 为 `https://511130-live-monitor-production.up.railway.app`。
- 2026-06-15: 增加 Railway `Dockerfile`、`railway.toml`、`/health` 和 `$PORT` 自动读取。
- 2026-06-15: 增加公式/PCF/利息/过期快照/状态优先级 focused tests。
- 2026-06-15: 强化 fail-closed 规则：PCF结构变化、CreationRedemptionUnit变化、缓存利息、过旧/不同步行情都不展示当前 a。
- 2026-06-15: 升级 `live_a_dashboard.py` 为手机/电脑团队看板。
- 2026-06-15: `/api/data` 返回状态分类、计算拆解、成分券明细、配置元数据。
- 2026-06-15: 增加 `/api/dates` 和 `/api/series`，支持近1分钟/5分钟/15分钟/1小时/今天范围切换。
- 2026-06-15: 曲线支持 1秒折线、1分钟 a值OHLC、15分钟 a值OHLC，并保留当前 a 与历史图表分离。
- 2026-06-15: 页面展示 a 值、300 距离、行情时间差、计算时间、飞书最近状态、阈值线、红色超阈值区间、手工利息标签。
- 2026-06-15: 增加 `/api/config`，支持保存目标日期、阈值和逐券利息覆盖。
- 2026-06-15: README 补充局域网团队访问命令。
- 2026-06-15: 挂载 Railway Volume `511130-live-monitor-volume` 到 `/data`，并设置 `A_MONITOR_RUNS_DIR=/data/runs`。

## Backlog

- 如果用户要求 511090 分钟级曲线，需要先确认可审计的成分券历史分钟价来源；否则只能保持日收盘口径。
- 观察首个真实自动化运行日：确认 `reports/511130_daily_actual_a/YYYYMMDD/` 和桌面 PDF 是否生成；若只有 `pending/*.md`，先看 PCF、逐券利息和分钟行情三层原因。
- 下一个交易时段复查：确认严格实时 a 是否恢复展示；若 `data_ok=false`，先看 `diagnostics.pcf/quote`，不要放宽 3 秒同步、30 秒新鲜度或缺利息不兜底规则。
- 若公网只用于团队查看，考虑设置 `A_MONITOR_PUBLIC_READONLY=1` 或访问控制，避免外部改配置。
- 若需要更强审计能力，可在 Railway Volume 之外再接数据库或对象存储。
- 若未来 PCF 换券，先人工确认新券结构，再改 `expected_component_codes`。
